{
  "name": "cra-generate-report",
  "version": "0.1.0",
  "private": true,
  "description": "Cyber Risk Advisory (CRA) report generator — stateless Cloudflare Worker that pulls live Trend Vision One v3.0 data and renders a PDF.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
